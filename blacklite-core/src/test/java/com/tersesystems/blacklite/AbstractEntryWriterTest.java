package com.tersesystems.blacklite;

import org.junit.jupiter.api.Test;

public class AbstractEntryWriterTest {

  @Test
  public void testArchive() {
    // Create a live database and insert some rows

    // Run archive

    // Check there's an archive database by that name

    // Check that those same rows are in the archive database

  }
}
